# config.yml
models:
  - type: main
    engine: ollama
    model: gemma4:12b 
    parameters:
      base_url: "http://host.docker.internal:11434/v1"

rails:
  input:
    flows:
      - mask sensitive data on input
  output:
    flows:
      - mask sensitive data on output

# Define the action for masking
actions:
  mask_sensitive_data:
    path: actions.py # We will use a simple inline script or define this