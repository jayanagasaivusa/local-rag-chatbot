# Frontend — Local RAG Chatbot

React (Vite) + Tailwind CSS UI for the Local RAG Chatbot. See the
[project root README](../README.md) for full setup instructions.

```bash
npm install
npm run dev
```
